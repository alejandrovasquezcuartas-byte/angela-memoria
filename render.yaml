services:
  - type: web
    name: angela-memoria
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn angela_server:app --host 0.0.0.0 --port 10000
    plan: free
